# Praxis App Surface

Route: `/app`

Walkthrough: type `send 0.5 sol to maya`, sign it, type `send 50 sol to maya`, type `swap 100 usdc into $SAFEMOON`, revoke the agent in Policy, type another send, then type `what's bonk doing this week`.
